name: Generate and Deploy Schedule Data

on:
  push:
    branches:
      - main
    paths:
      - 'schedule-generator/schedule.xlsx'  # 修改点1：更新监控路径，正确指向schedule-generator目录下的Excel文件
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    environment:
      name: github-pages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # 修改点2：添加调试步骤，帮助诊断文件系统问题
      - name: Debug filesystem
        run: |
          pwd
          echo "仓库根目录内容:"
          ls -la
          echo "schedule-generator目录内容:"
          ls -la schedule-generator/

      - name: Setup Node.js with Caching
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: schedule-generator/package.json

      - name: Install dependencies
        run: npm install
        working-directory: ./schedule-generator

      - name: Generate data.json from Excel
        run: node process-excel.js
        working-directory: ./schedule-generator
      
      - name: Prepare deployment artifact
        run: |
          mkdir -p _site
          cp index.html _site/
          cp schedule-generator/data.json _site/
          # 修改点3：添加students.json如果您的脚本也生成了这个文件
          cp schedule-generator/students.json _site/ || echo "No students.json file found"
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
        
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './_site'
          
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
